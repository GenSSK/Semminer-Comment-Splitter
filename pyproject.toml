[project]
name = "semminer-comment-splitter"
version = "0.1.0"
description = "セミナーコメント集計ツール"
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=5.2.2",
    "tkinterdnd2>=0.4.3",
]

[project.scripts]
app = "app:main"

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
