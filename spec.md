# セミナーコメント集計ツール 仕様書

## 概要

Manaba からエクスポートしたコメント ZIP を発表者別の CSV に分割するデスクトップツール。  
発表者は CSV から自動検出し、GUI 上で確認・修正できる。

---

## 対象環境

| 項目 | 内容 |
|------|------|
| OS | Windows 10/11、macOS 11+ |
| ランタイム | Python 3.10+ |
| 配布形式 | Python スクリプト（または PyInstaller 単一 exe） |

---

## 使用ライブラリ

| 用途 | ライブラリ |
|------|-----------|
| GUI | CustomTkinter |
| ドラッグ&ドロップ | tkinterdnd2（オプション、未インストール時はボタン操作のみ） |
| CSV / ZIP 処理 | 標準ライブラリ（csv, zipfile, io） |

---

## 動作フロー

```
ZIP 入力 → 発表者リスト確認・編集 → 集計実行 → CSV 出力
```

### Step 1: ZIP 読み込み

- ドロップゾーンへのドラッグ&ドロップ（tkinterdnd2 インストール時）
- 「ZIP を選択…」ボタンによるファイル参照
- 読み込み後、ZIP と同じフォルダを出力先に自動設定

### Step 2: 発表者リスト

自動検出ロジック（Q.No ごとに以下の順に判定）:

1. コメントが空欄 → その Q.No の発表者
2. コメント先頭行が自分の学籍番号に一致（大文字小文字無視）
3. コメントに「発表者は自分」を含む

検出結果は GUI テーブルに表示。各行の学籍番号・氏名を直接編集可能。  
行の追加・削除も可能（Q.No を手動で割り当て）。

### Step 3: 集計実行

| 処理 | 内容 |
|------|------|
| 発表者除外 | 発表者自身のコメント行はスキップ |
| 空コメント除外 | コメント本文が空の行はスキップ |
| 学籍番号プレフィックス除去 | コメント先頭行が学籍番号パターン（他者の ID を書いた場合）は削除 |
| 出力ファイル名 | `集計コメント_{学籍番号}_{氏名}.csv` |
| エンコーディング | UTF-8 BOM 付き |

---

## 入力データ形式

### ZIP 構造（Manaba エクスポート）

```
*.zip
├── comments.csv        ← 使用（UTF-8 BOM 付き）
├── comments-sjis.csv   ← 使用しない
├── answer.csv          ← 使用しない
└── answer-sjis.csv     ← 使用しない
```

### comments.csv の構造

```
レポート/記述式問題の採点      ← メタデータヘッダー（5行）
<タイムスタンプ>
<課題名>
contents_id,<hash>
                               ← 空行
氏名,ユーザ ID,Q.No,report/answer,corrected_file,提出日,成績,点数  ← 列ヘッダー
<学生名>,<学籍番号>,<Q.No>,<コメント本文>,...
```

### 学籍番号パターン

正規表現: `^[0-9]{2}[A-Za-z]{1,5}[0-9]{1,3}$`（大文字小文字無視）  
例: `25UDE02`, `26KMH01`, `25ude91`, `26km01`

---

## 出力 CSV 形式

```csv
コメント
<コメント本文1>
<コメント本文2>
...
```

エンコーディング: UTF-8 BOM 付き（Windows Excel でダブルクリック時に文字化けなし）

---

## GUI レイアウト

```
┌────────────────────────────────────────┐
│  セミナーコメント集計ツール             │
├────────────────────────────────────────┤
│ ① ZIPファイル                          │
│  ┌──────────────────────────────────┐  │
│  │  ドロップゾーン                   │  │
│  └──────────────────────────────────┘  │
│  [ZIPを選択…]  <ファイル名>            │
├────────────────────────────────────────┤
│ ② 発表者リスト          [＋行追加]    │
│  Q.No  学籍番号    氏名               │
│  Q.1   25UDE02   hoge hoge   [×]      │
│  Q.2   25UDE91   hoge hoge   [×]      │
│  ...                                   │
├────────────────────────────────────────┤
│ ③ 出力先フォルダ                       │
│  [フォルダを選択…]  <パス>             │
├────────────────────────────────────────┤
│         [集計を実行]                   │
├────────────────────────────────────────┤
│ ログ                                   │
│  ✅ 読み込み完了 (XX件)               │
│  ✅ 集計コメント_25UDE02_hogehoge.csv  │
│  ...                                   │
└────────────────────────────────────────┘
```
